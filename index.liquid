extends: default.liquid

title: Johann's Notes
route: blog
---

<ul class="blog-list">
  {% for post in posts %}
    <li>
      <a href="{{ post.path }}">{{ post.title }}</a>
    </li>
  {% endfor %}
</ul>
