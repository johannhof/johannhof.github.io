<!DOCTYPE html>
<html>
  <head>
    {% include "_head.liquid" %}
  </head>
<body data-route={{ page.data.route }}>
  {% include "_menu.liquid" %}
  <main>
    <article>
      <h1>{{ page.title }}</h1>
      <time pubdate="pubdate">{{ page.published_date | date: "%b %d, %Y" }}</time>
      {{ page.content }}
    </article>
  </main>
  {% include "_footer.liquid" %}
</body>
</html>
